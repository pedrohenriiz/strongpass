import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  button: {
    padding: 20,
    width: '100%',
    borderRadius: 4,
  },
  buttonText: {
    fontSize: 16,
    fontFamily: 'Roboto',
    fontWeight: 'bold',
    color: '#ffffff',
    textAlign: 'center',
  },
});

export default styles;
