import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  label: {
    fontFamily: 'Roboto',
    fontSize: 14,
    fontWeight: 'bold',
    color: '#695ECA',
    marginBottom: 4,
  },
});

export default styles;
